<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="zad7.RegisterPage">
    <ContentPage.Content>
   <ScrollView>
         <VerticalStackLayout
             Padding="30,0"
             Spacing="25">
                <Label
                    Text="Register"
                    FontSize="Title"
                    HorizontalOptions="Center" />
                <Entry
                    Text="{Binding Email}"
                    Placeholder="Email" />
                <Entry
                    Text="{Binding Username}"
                    Placeholder="Username" />
                <Entry
                    Text="{Binding Password}"
                    Placeholder="Password"
                    IsPassword="True"/>
                <Entry
                    Text="{Binding RepeatPassword}"
                    Placeholder="Repeat Password"
                    IsPassword="True"/>
                <Label Text="{Binding ErrorMessage}" TextColor="Red"></Label>
                <Button Command="{Binding SubmitCommand}" Text="Register"></Button>
                <Label Text="Already have an account?"></Label>
                <Button Command="{Binding LoginCommand}" Text="Login"></Button>
                
         </VerticalStackLayout>
     </ScrollView>       
    </ContentPage.Content>
</ContentPage>